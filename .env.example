# Airtable personal access token (read scope on the NXG Site base). Keep this SECRET.
AIRTABLE_TOKEN=pat_xxxxxxxxxxxxx
# Base ID (not secret). Defaults to the NXG base if omitted.
AIRTABLE_BASE_ID=appmiX1Oz2OgbpuZ0
